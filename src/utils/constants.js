const MAX_RETRIES = 10;
const DEFAULT_INCREASING_INTERVAL = 1000;
const MAX_RETRIES_ERROR = 'Max number of retries reached';

module.exports = {
  MAX_RETRIES,
  DEFAULT_INCREASING_INTERVAL,
  MAX_RETRIES_ERROR,
};
