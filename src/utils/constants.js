const MAX_RETRIES = 10;
const DEFAULT_RETRY_FACTOR = 1000;
const MAX_RETRIES_ERROR = 'Max number of retries reached';

module.exports = {
  MAX_RETRIES,
  DEFAULT_RETRY_FACTOR,
  MAX_RETRIES_ERROR,
};
